# By contributor — the methodologies they used

<!-- freeports-validate:begin -->
<!-- Written by `freeports-validate report`. Anything between the markers is replaced. -->

### By contributor, listing the methodologies they used

The same view as `freeports-validate granted-by <contributor>`, for the whole repository at once.

2 grants by 1 contributor under 1 methodology; coverage 2/571 (0%); check-grants: passing

*Generated by `freeports-validate report`, and demonstrative: it shows what this repository's validation documents claim as of one run, not that the data is correct. To be sure of any line, run `freeports-validate check-grants` yourself.* [What these claims are worth: the general methodology](https://docs.freeports.org/en/stable/validation/general_methodology.html).

| Contributor | Methodology | State |
| --- | --- | --- |
| Oreste Sciacqualegni | [basic check](https://docs.freeports.org/en/latest/_sources/validation/methodologies/basic_check.rst.txt) | current |

<!-- freeports-validate:end -->

---

Everything between the two markers above is **generated**, and is replaced whole every time the
report is refreshed — so nothing written between them survives. It is the same view as
`freeports-validate granted-by <contributor>`, for the whole repository at once instead of for one file, one contributor or one
methodology.

This repository's `pre-commit` hook rewrites it. By hand:

```sh
freeports-validate report --format markdown --table contributor-methodology --out validation/report/contributor-methodology.md
```

[← the repository's README](../../README.md) · [the other tables](.)
